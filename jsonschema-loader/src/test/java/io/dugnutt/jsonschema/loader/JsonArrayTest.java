package io.dugnutt.jsonschema.loader;

import org.junit.Assert;
import org.junit.Test;

/**
 * @author erosb
 */
public class JsonArrayTest {

    // private static final LoadingState emptyLs = JsonValueTest.emptyLs;

    @Test
    public void testForEach() {
        // JsonObject rawObj = JsonUtils.blankObject();
        // JsonArray subject = JsonProvider.provider()
        //         .createArrayBuilder(asList(true, rawObj))
        //         .build();
        //
        // final Consumer<JsonValue> iterator = mock(Consumer.class);
        // subject.forEach(iterator);
        // LoadingState childState0 = emptyLs.childFor(0), childState1 = emptyLs.childFor(1);
        // verify(iterator).accept(0, JsonValue.of(true, childState0));
        // verify(iterator).accept(1, JsonValue.of(rawObj, childState1));
        Assert.fail("Not implemented");
    }
}
